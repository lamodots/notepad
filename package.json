{
  "homepage": "https://lamodots.github.io/notepad",
  "name": "notepad",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start-json-server": "json-server --watch ./db/db.json",
    "watch": "webpack --watch",
    "serve": "webpack-dev-server",
    "dev-start": "webpack --watch webpack --watch json-server --watch ./db/db.json -port 8000",
    "build": "webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-loader": "^9.4.4",
    "typescript": "^5.2.2",
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "json-server": "^0.17.3",
    "webpack-dev-server": "^4.15.1"
  }
}
